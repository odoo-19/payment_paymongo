<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Link PayMongo provider to QRPH payment method -->
    <record id="payment_provider_paymongo" model="payment.provider">
        <field name="payment_method_ids" eval="[(4, ref('payment_paymongo.payment_method_qrph'))]"/>
    </record>

</odoo>
